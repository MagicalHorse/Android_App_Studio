<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="style_horizontal_line" parent="AppTheme">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0.1dip</item>
        <item name="android:background">#66999999</item>
    </style>

    <style name="backgroundstyle" parent="AppTheme">
        <item name="android:background">@color/color_lightgrey</item>
        <item name="android:layout_height">@dimen/titleheight</item>
        <item name="android:layout_width">match_parent</item>
    </style>


    <!--主页 底部按钮-->
    <style name="background_gray" parent="backgroundstyle">
        <item name="android:padding">@dimen/padding_style</item>
    </style>

    <!--头部标题-->
    <style name="titlestyle" parent="backgroundstyle">
        <item name="android:padding">@dimen/titlepadding</item>
    </style>


    <style name="CustomDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
    </style>

    <style name="CustomProgressDialog" parent="@style/CustomDialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!--定义 内容视图距离俯视图的距离-->
    <style name="contentpadding" parent="AppTheme">
        <item name="android:background">@drawable/rect_blacksoild_while_background</item>
    </style>

    <!--定义 倒计时 时间 的样式-->
    <style name="timestyle" parent="AppTheme">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">@dimen/padding_style</item>
        <item name="android:background">@color/color_deeoyellow</item>
        <item name="android:textColor">@color/color_white</item>
        <item name="android:textSize">11sp</item>
    </style>

</resources>
